/**
 * Jest setup file
 * Loads the StorageSync library into the global scope before tests run
 */

// Load the library
require('../src/index.js');

// Optional: Add custom matchers or global test setup here
